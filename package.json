{
  "name": "ai-venture-builder",
  "version": "0.3.0",
  "private": true,
  "description": "Demand-first venture builder: validate paid pain first, then find zero-cost public assets for an MVP.",
  "type": "module",
  "scripts": {
    "qa": "node scripts/qa-v3.mjs",
    "inventory:legacy": "node scripts/full-inventory.mjs --dry-run"
  },
  "engines": {
    "node": ">=20"
  }
}
